---
status: accepted
---

# 当前 Excel 模板作为迁移权威

系统的参数含义、单位、默认值和计算关系以当前暖通 Excel 模板为准，系统计算规则必须对其中的有效公式作等价实现。工作表行列布局、页面分组、录入顺序和交换文件结构不受模板约束，可以按新的交互逻辑设计，但不得改变计算语义或结果。

## Consequences

- 计算验收需要使用相同输入对比系统与模板的结果。
- 系统不需要复刻模板的工作表、单元格、颜色和固定房间列。
- 模板中的断链公式和未定义关系必须显式列为待确认项，不能由开发人员自行猜测。
